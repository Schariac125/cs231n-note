# 一些可能有用的

## 1.Softmax和归一化的编写

```
N=x.shape[0]
scores=x-np.max(x,axis=1)[:,None]
p=np.exp(scores)
p /= np.sum(p, axis=1)[:,None]
loss=-np.sum(np.log(p[np.arange(N),y]))/N
```

如果用 Softmax 做损失函数，那么最外层的反向传播就是这么写的，再拷贝一下就行

```
dx=p.copy()
dx[np.arange(N),y] -= 1
dx /= N
```

